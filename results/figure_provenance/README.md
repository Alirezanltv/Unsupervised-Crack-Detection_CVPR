# Provenance of the qualitative figure (main paper Fig. 3)

Generated by p3_qualitative/make_figure.py; rows are the sorted test stems
at fixed indices 0/59/118/177/236, chosen blind to all outputs.

Maps used:
- ours, RD4AD, EfficientAD, DRAEM: the banked Kaggle runs (ours re-dumped
  locally on CPU from the banked seed-0 checkpoint; eval reproduces the
  banked metrics exactly: AUROC 0.9307 / AP 0.4883).
- PatchCore: CPU refit at seed 0 -- reproduces the banked run exactly
  (0.8327 / 0.1932; patchcore_s0_cpu_refit.json).
- PaDiM: CPU refit at seed 0 -- statistically equivalent, not identical
  (0.9105 / 0.2508 vs banked 0.9175 / 0.2730), because PaDiM's random
  channel subsampling depends on the device RNG stream; the difference is
  within the method's variability and is disclosed in the figure caption
  (padim_s0_cpu_refit.json).
